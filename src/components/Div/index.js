import styled from 'styled-components';
import {
  space,
  color,
  layout,
  border,
  background,
  position,
} from 'styled-system';

const Div = styled.div`
  ${space}
  ${color}
  ${layout}
  ${border}
  ${background}
  ${position}
`;

export default Div;
